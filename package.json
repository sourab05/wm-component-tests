{
  "name": "wm-component-tests",
  "version": "1.0.0",
  "description": "Data-driven Playwright framework for WaveMaker Studio widget property testing (canvas + preview)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:widget": "playwright test src/tests/widget-properties.spec.ts",
    "pipeline": "tsx scripts/run-widget-pipeline.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-storage": "^3.700.0",
    "@playwright/test": "^1.49.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "qs": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/qs": "^6.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
